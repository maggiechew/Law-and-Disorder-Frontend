import React from 'react'

function FilterBar(props) {
    const {crimeFilters, setCrimeFilters, timeFilters, setTimeFilters} = props;
    const potentialCrimes = [];
    const potentialTimes = [];


  return (
    <div>FilterBar</div>
  )
}

export default FilterBar